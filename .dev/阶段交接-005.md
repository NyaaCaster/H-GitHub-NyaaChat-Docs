# NyaaChat-Docs V1 阶段交接 005（P0–P4 完成 → P5 待续）

## 交接目的

本文件用于 P0–P4 收尾与 P5 续接。新对话开始时必读：

1. `.dev/开发计划-SSOT.md` — 唯一事实来源（信息架构、阶段划分、术语约定、红线）。
2. 本文件「当前进度」「续接提示词」两节。

## 当前进度（P0–P4 已完成）

| 阶段 | 内容 | 提交 |
|------|------|------|
| P0 | 结构骨架：28 文档、config.mjs nav/sidebar 重排、清 features 占位 | `2f04c42` |
| P1 | 开始使用 ×5：what-is-nyaachat / quick-start / interface-tour / configure-model / first-chat | `54cfb04` |
| P2 | 账号系统 ×6：index / login-register / profile / catfood-recharge / quota-expand / cloud-sync | `60cfee4` |
| P3 | 角色扮演·基础 ×6：index / character-selection / create-character / user-role / import-export / shared-library | `14225fd` |
| P4 | 角色扮演·进阶 ×3：world-info / regex / variables | `0d63cec` |

- ✅ 已撰写正文：20 个文档（guide×5 + account×6 + roleplay×9）
- ⬜ 仅骨架无正文：6 个（knowledge-base×6）
- ⬜ 待收尾：faq.md（骨架）、index.md 首页（P0 初版，P6 终稿）

关键产出已在正文中沉淀：
- 全站术语（顶部工具栏/弹窗/猫粮/QinyAPI=QingAPI/世界书=WorldInfoRule/软设定·硬约束）
- 扩容价格表（15/10/5/5/5 猫粮）
- 导入导出格式对比（NyaaChat 原生 vs ST chara_card_v3）
- NyaaAcount 登录错误码映射

## 本轮无改动文件

仅写交接文档，不涉及 doc-files 内容变更。

## 已知待处理

- P5 knowledge-base/ 6 文档需要逐页源码探索（KnowledgeBaseModal、EmbeddingConfigModal、KnowledgeBaseEditModal、KnowledgeBaseSelectModal），确认嵌入模型配置字段、知识库与文档管理流程、角色绑定的 linkedKbIds 机制、KB 栈额度逻辑
- 部署脚本已知问题：`sync-docs.py` 已修复为 rsync（commit `df32b2f` 之后），macmini git pull 在 Session 0 中偶尔超时，需用 scp 直接传文件替代
- 推送用 GitHub PAT 内联方式（`passwds.md`），不走凭证管理器

## 续接提示词

> 继续开发 NyaaChat-Docs 用户文档站，进入 **P5（知识库）**，以 plan 模式推进。
>
> 必读：`H:\GitHub\NyaaChat-Docs\.dev\开发计划-SSOT.md`（信息架构/术语/红线/阶段批次）、`.dev\阶段交接-005.md`（本文件，当前进度）。
>
> 当前：P0–P4 已完成，20 个文档有正文。P5 要写 `knowledge-base/` 下 6 个文档正文：`index.md`（什么是知识库/RAG 通俗解释）`prerequisites.md`（登录+嵌入模型配置）`manage.md`（创建与管理知识库）`documents.md`（上传与管理文档）`link-to-character.md`（绑定角色与检索注入）`quota.md`（知识库栈额度与扩容）。
>
> 第一件事：读 `H:\GitHub\NyaaChat\src\components\KnowledgeBaseModal.tsx`、`KnowledgeBaseEditModal.tsx`、`EmbeddingConfigModal.tsx`、`KnowledgeBaseSelectModal.tsx` 等组件源码，核对知识库的实际 UI 字段、弹窗标题、交互流程，再动笔。嵌入模型供应商独立于对话/图片供应商体系（QinyAPI 也为嵌入提供了接入点），需确认字段名和配置步骤。
>
> 约束：术语沿用 P1 确立的统一用词（顶部工具栏/弹窗/猫粮/世界书/软设定·硬约束）；QinyAPI 为主线示例、界面标示 QingAPI；只动 `doc-files/knowledge-base/*.md` 与 SSOT，不重建镜像；私有仓库地址与密钥禁止写入文档。
>
> 部署：内容更新后用 GitHub PAT 内联 `git push`（PAT 在 `H:\GitHub\passwds.md`）；macmini git pull 若超时则改用 `scp` 直传文件到 `/root/DockerContainer/NyaaChat-Docs/doc-files/knowledge-base/` + repo 同步 + `docker compose restart`。提交前 `git status` 检查秘密，明确 `git add <file>`。
