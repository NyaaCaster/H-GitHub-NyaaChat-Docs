# NyaaChat-Docs V1 阶段交接 001（P0 结构骨架）

## 交接目的

本文件用于 P0（文档结构骨架）收尾与 P1 续接。开始 P1 前必读：

1. `.dev/开发计划-SSOT.md` — 唯一事实来源（信息架构、阶段划分、术语约定、红线）。
2. 本文件「当前进度」「续接提示词」两节。

## 当前进度（P0 已完成）

- ✅ 清理占位：删除 `doc-files/features/` 目录；`index.md` 去掉「扩展系统」卡片、改为四大板块 feature 卡；`faq.md`、`guide/quick-start.md` 由旧占位内容改写为骨架。
- ✅ 建立结构：按 SSOT 目标目录树创建全部骨架文档，仅含标题层级、无正文。
  - `guide/` ×5：`what-is-nyaachat` `quick-start` `interface-tour` `configure-model` `first-chat`
  - `roleplay/` ×9：`index` `character-selection` `create-character` `user-role` `import-export` `shared-library` `world-info` `regex` `variables`
  - `knowledge-base/` ×6：`index` `prerequisites` `manage` `documents` `link-to-character` `quota`
  - `account/` ×6：`index` `login-register` `profile` `catfood-recharge` `quota-expand` `cloud-sync`
  - 首页 `index.md`（home 布局）+ `faq.md`
  - 合计 28 个 `.md`，27 个内容页共 211 个标题。
- ✅ 导航：`doc-files/.vitepress/config.mjs` 的 nav 改为「首页 / 开始使用 / 角色扮演 / 知识库 / 账号系统 / 常见问题」，sidebar 按 `/guide/`、`/roleplay/`、`/knowledge-base/`、`/account/` 四组重排，items 与目录树一致。
- ✅ 保留资源未动：`public/logo.svg`、`.vitepress/theme/**`。

## 本轮改动清单

| 文件 | 改动 |
|------|------|
| `doc-files/features/index.md` | 删除（不在目标目录树） |
| `doc-files/index.md` | 改写首页 hero + 四大板块 feature 卡，去除扩展 |
| `doc-files/faq.md` | 占位内容 → 骨架标题 |
| `doc-files/guide/quick-start.md` | 占位内容 → 骨架标题 |
| `doc-files/guide/*.md`（新建 ×4） | what-is-nyaachat / interface-tour / configure-model / first-chat |
| `doc-files/roleplay/*.md`（新建 ×9） | 见上 |
| `doc-files/knowledge-base/*.md`（新建 ×6） | 见上 |
| `doc-files/account/*.md`（新建 ×6） | 见上 |
| `doc-files/.vitepress/config.mjs` | nav / sidebar 按四大板块重排 |
| `.dev/开发计划-SSOT.md` | P0 子项状态置 ✅ |

## 仍需继续验证 / 已知问题

- 骨架仅有标题层级，全部正文待 P1–P6 逐批填充。
- 各页标题层级为初稿设计，正文撰写时若需调整须先改 SSOT 再改文档。
- 侧边栏链接与文件已一一对照，未发现死链；正文完成后 P6 再做一次全量导航校对。

## 续接提示词

> 继续开发 NyaaChat-Docs 用户文档站，进入 **P1（开始使用）**，以 plan 模式推进。
>
> 必读：`H:\GitHub\NyaaChat-Docs\.dev\开发计划-SSOT.md`（信息架构/术语/红线）、`.dev\阶段交接-001.md`（当前状态）。
>
> 当前：P0 结构骨架已完成并部署上线，28 个文档仅有标题、无正文。P1 要写 `guide/` 下 5 个文档正文：`what-is-nyaachat` `quick-start` `interface-tour` `configure-model` `first-chat`。
>
> 第一件事：读 `H:\GitHub\NyaaChat\` 前端组件源码，核对顶栏两行图标、弹窗名与真实界面文案，确立全站通用术语，再动笔。
>
> 约束：以「顶栏图标 → 弹窗」真实交互描述，不写成模块页；对话模型配置以 QinyAPI 为主线示例；术语统一（顶部工具栏/弹窗/猫粮/世界书/软设定·硬约束）；只动 `doc-files/*.md` 与 `config.mjs`，不重建镜像；私有仓库地址与密钥禁止写入文档；未经明确请求不 commit/push。
>
> 部署：内容更新走 `git push` → macmini `python3 sync-docs.py`（HMR 生效，无需重建镜像）。
